# SmartBI Frontend

Responsive static pages served by Flask. The pages consume the JSON APIs exposed by the backend.

## Dashboard Usage

Open `/dashboard` after signing in as admin. The dashboard loads the latest upload by default and can switch uploads from the selector in the sidebar.